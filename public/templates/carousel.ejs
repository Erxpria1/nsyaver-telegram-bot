<div class="carousel-container">
  <!-- Slayt 1 -->
  <div class="carousel-slide">
    <% if (logo) { %>
      <div class="slide-logo">
        <img src="<%= logo %>" alt="Logo">
      </div>
    <% } %>

    <h1 class="slide-title"><%= motto %></h1>
    <div class="slide-number">01</div>
  </div>

  <!-- Slayt 2 -->
  <div class="carousel-slide">
    <h2 class="slide-title">🎯 Amacımız</h2>
    <div class="slide-mission">
      <p><%= mission %></p>
    </div>
    <div class="slide-number">02</div>
  </div>

  <!-- Slayt 3 -->
  <div class="carousel-slide">
    <div class="slide-message">
      <p><%= message %></p>
    </div>
    <div class="slide-footer">
      🌟 Konya ıh.com 🌟
    </div>
    <div class="slide-number">03</div>
  </div>
</div>

<style>
  .carousel-container {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    text-align: center;
  }

  .carousel-slide {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 30px 20px;
    background: white;
    border-radius: 10px;
  }

  .slide-logo {
    margin-bottom: 25px;
  }

  .slide-logo img {
    width: 200px;
    height: 200px;
    object-fit: contain;
    border-radius: 10px;
  }

  .slide-title {
    font-size: 2em;
    font-weight: 700;
    margin-bottom: 20px;
    color: #333;
    line-height: 1.3;
  }

  .slide-mission {
    font-size: 1.1em;
    color: #555;
    line-height: 1.6;
    max-width: 400px;
  }

  .slide-message {
    font-size: 1.1em;
    color: #555;
    line-height: 1.6;
    max-width: 400px;
  }

  .slide-footer {
    font-size: 1em;
    color: #999;
    font-weight: 500;
    margin-top: 20px;
  }

  .slide-number {
    margin-top: 30px;
    font-size: 0.9em;
    color: #999;
    font-weight: 500;
  }
</style>